export default function Architecture(){return <div className="page"><div className="topline"><div><h1>Architecture</h1><p className="subtle">One governed memory workflow, replaceable adapters for mock and live modes.</p></div><span className="badge active">Production-minded MVP</span></div><div className="card diagram"><div className="node"><b>Next.js</b><p className="subtle">Agent workspace & trace</p></div><div className="node"><b>FastAPI</b><p className="subtle">Scoped memory services</p></div><div className="node"><b>Amazon Bedrock</b><p className="subtle">Extract, reason, consolidate</p></div><div className="node"><b>CockroachDB</b><p className="subtle">Records + vector index</p></div></div><div className="section twoCol"><div className="card"><h2>Retrieval lifecycle</h2><p className="subtle">Query embedding → project-scoped vector candidates → eligibility rules → transparent weighted rerank → 4–8 compact memories → Bedrock response → persisted trace and access update.</p></div><div className="card"><h2>Evidence lifecycle</h2><p className="subtle">Immutable event → validated memories and provenance → consolidation proposal → canonical memory → originals marked merged/superseded → large payload archived in S3 by Lambda.</p></div></div><div className="section card"><h2>Trust boundaries</h2><p className="subtle">Retrieved memory is delimited as untrusted data. Tenant scope is enforced server-side. Model output is schema-validated. Writes are audited and never initiated by instructions found inside memory content.</p></div></div>}
